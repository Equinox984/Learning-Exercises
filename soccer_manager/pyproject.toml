[project]
name = "soccer-manager"
version = "2.0.0"
description = "Honduran Soccer Manager CLI Application"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
